#include <bits/stdc++.h>
using namespace std;

float subtract(float a, float b) {
    return a-b;
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    cout << subtract(19.7, 11.2);

    return 0;
}