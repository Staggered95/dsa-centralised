#include <bits/stdc++.h>
using namespace std;

class Tree 
{
    class Node
    {
        public:
        Node* left;
        int data;
        Node* right;
            Node(int val) : left(nullptr), data(val), right(nullptr) {}
    }

    Node* root;

    Tree() : root(nullptr) {}

    
}

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    

    return 0;
}