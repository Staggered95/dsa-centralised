#include <bits/stdc++.h>
using namespace std;

/*
A BST is a special type of tree where-
    Every node has at most 2 children.
    For every node, all values in Left-subtree are smaller, and all values in Right-subtree are greater.
    Both LST and RST are also BST

    Inorder traversal of a BST is always sorted
*/

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    

    return 0;
}